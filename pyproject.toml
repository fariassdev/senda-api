[project]
name = "senda"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-genai==1.28.0",
    "pyaudio==0.2.14",
    "python-dotenv==1.1.1",
    "requests==2.32.4",
    "pydub==0.25.1",
    "audioop-lts==0.2.2",
]

[tool.setuptools.packages.find]
where = ["src"]

[dependency-groups]
dev = [
    "pre-commit==4.2.0",
    "ruff==0.12.7",
]
